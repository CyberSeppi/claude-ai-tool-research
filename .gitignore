node_modules/
package-lock.json
!app/package-lock.json
.superpowers/
.claude/scheduled_tasks.*

# Generated research output — regenerated by the skill, not committed.
# (raw-records.json is the LLM's verified findings; report.{json,md} are
# rebuilt from it via `npm run research:build`. All three are time-sensitive
# snapshots, so we keep them out of git.)
data/raw-records.json
data/report.json
data/report.md

# Runtime state
app/db/
.claude/projects/
*.log
