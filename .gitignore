# ─── Dependencies & build output ─────────────────────────────────────
node_modules/
**/node_modules/
package-lock.json
!app/package-lock.json
dist/
build/
.cache/
.parcel-cache/
.vite/
.turbo/
.next/
out/
*.tsbuildinfo

# ─── Test & coverage artifacts ───────────────────────────────────────
coverage/
.nyc_output/
*.lcov
**/__pycache__/
.pytest_cache/
playwright-report/
.playwright/
test-results/

# ─── Editor / IDE / OS noise ─────────────────────────────────────────
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ─── Logs ────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ─── Secrets — actual env file is per-machine; template is committed ──
.env
.env.local
.env.*.local

# ─── Runtime / app state ─────────────────────────────────────────────
app/db/

# ─── Claude Code workspace state ─────────────────────────────────────
.claude/scheduled_tasks.*
.claude/projects/
.claude/cache/
.claude/state/
.superpowers/

# ─── Local research progress notes (per-session, not for the repo) ───
PROGRESS.md
NOTES.md
TODO.local.md
*.local.md

# ─── Generated research output — regenerable via the skill ───────────
# raw-records.json is the LLM's verified findings; report.{json,md} are
# rebuilt from it via `npm run research:build`. All three are
# time-sensitive snapshots, so they stay out of git.
data/raw-records.json
data/report.json
data/report.md

# Workflow / agent transcripts that may end up here from local debug runs
*.workflow.jsonl
*.transcript.jsonl

# ─── Local sandbox / scratch ─────────────────────────────────────────
tmp/
